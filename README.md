# Introduction

This workspace contains two crates of note:

- [`mdns-reactor`](./mdns-reactor/), which provides an asynchronous mDNS server and client implementation for any agnostic runtime.
- [`mdns-proto`](./mdns-proto/), which provides a `no_std` implementation of the mDNS protocol.
